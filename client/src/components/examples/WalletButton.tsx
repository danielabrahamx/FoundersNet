// This example is outdated - the app now uses WalletConnectButton with Solana
// import WalletButton from '../WalletButton';
// import { useState } from 'react';

export default function WalletButtonExample() {
  // const [address, setAddress] = useState<string>();

  return (
    <div className="p-4 bg-background">
      <p className="text-muted-foreground">Wallet button example - see WalletConnectButton component instead</p>
    </div>
  );
}
