# Solana TestNet Environment Configuration
# TestNet is a stable environment for pre-production testing

# Node Environment
NODE_ENV=staging

# API Configuration
VITE_API_URL=https://your-testnet-api-domain.com

# Solana Network Configuration
VITE_SOLANA_NETWORK=solana-testnet
VITE_SOLANA_RPC_URL=https://api.testnet.solana.com
VITE_SOLANA_WS_URL=wss://api.testnet.solana.com
VITE_SOLANA_EXPLORER_URL=https://explorer.solana.com/?cluster=testnet
VITE_SOLANA_COMMITMENT=confirmed

# Solana Program Configuration
# Replace with your deployed program ID after running: anchor deploy --provider.cluster testnet
VITE_SOLANA_PROGRAM_ID=YourTestNetProgramIDHere

# Admin Configuration
# Replace with your admin wallet public key
VITE_SOLANA_ADMIN_ADDRESS=YourAdminWalletPublicKeyHere

# Optional: Database
DATABASE_URL=postgresql://user:password@localhost:5432/foundersnet_testnet

# Optional: WalletConnect (if using)
VITE_WALLETCONNECT_PROJECT_ID=your_project_id_here

# Testing Configuration
VITE_ENABLE_DEBUG_LOGGING=true
